
@*@{
    ViewBag.Title = "Home";
}*@

<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Stu</title>
    </head>
    <body>

        @using (Html.BeginForm("Login", "Home", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div>
                <button type="submit">Logout</button>
            </div>
        }
        @using (Html.BeginForm("CourseSchedule", "Student", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div>
                <button type="submit">Course Schedule</button>
            </div>
        }
        @using (Html.BeginForm("ExamSchedule", "Student", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div>
                <button type="submit">Exam Schedule</button>
            </div>
        }
    </body>

</html>
