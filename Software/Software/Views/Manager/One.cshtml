@model Software.Models.User

@{
    ViewBag.Title = "Work!!";
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>One</title>
</head>
<body>
    <h2>One work!!</h2>

    @using (Html.BeginForm("Home", "Manager", FormMethod.Post))
    {

        <select id="Select1">
            
            <option>User.ID.tostring()</option>
            
        </select>
        @Html.AntiForgeryToken()
        <div>
            <button type="submit">Back</button>
        </div>
    }
</body>
</html>

